<resources>
    <style name="Theme.CampusPlanPoc" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <!--
            ライト/ダークは端末設定に追従する。システムバーのアイコン色は
            enableEdgeToEdge() が背景の明暗を見て自動で切り替えるので、ここでは固定しない。
        -->
    </style>
</resources>
